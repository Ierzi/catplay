[project]
name = "catplay"
version = "1.2.0"
description = "python music player idk"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["pyside6", "shiboken6", "mutagen", "pypresence", "dotenv", "requests", "pylast", "pyinstaller"]
