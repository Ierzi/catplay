[project]
name = "catplay"
version = "2.0.0"
description = "python music player idk"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["pyside6", "shiboken6", "mutagen", "pypresence", "requests", "pylast", "pyinstaller"]
